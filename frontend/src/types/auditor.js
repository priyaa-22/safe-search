/**
 * @typedef {Object} AuditorShape
 * @property {string} id - Unique identifier for the auditor registry.
 * @property {string} name - Name of the auditor.
 * @property {string} organization - The auditing organization.
 * @property {string} email - Email address.
 * @property {number} activeKeyVersion - Currently active key pair version.
 * @property {string} status - Access status (Active, Locked).
 * @property {string} registeredAt - Registration date string (YYYY-MM-DD).
 */
export const AuditorShape = {};
